using UnityEngine;
using System.Collections;

public class SlidingMapCanvasController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		gameObject.SetActive (false);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
